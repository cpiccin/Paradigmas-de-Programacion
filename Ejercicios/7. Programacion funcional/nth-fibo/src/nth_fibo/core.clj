(ns nth-fibo.core
  (:gen-class))

;; Definir la función nth-fibo que reciba un número entero no negativo y devuelva el correspondiente 
;; término de la sucesión de Fibonacci.


;(defn nth-fibo [n]
;  (cond
;    (= n 0) 0
;    (= n 1) 1
;    :else (+ (nth-fibo (- n 1)) (nth-fibo (- n 2)))
;))
;
;
;(defn -main
;  [& args]
;  (println(nth-fibo 11)))

